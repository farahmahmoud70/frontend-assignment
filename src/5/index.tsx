import { FunctionComponent } from "react";

// Components
import Input from "./components/Input";
import List from "./components/List";

const Task4: FunctionComponent = () => {
  // Hello There I'm sorry I have just things into common component from the beginning
  // as I had a quick review that there is a common component
  // but did read each task until I started working on it, so from the beginning I had common components
  // so this task already resolved in task 3
  return (
    <div>
      <Input />
      <br />
      <List />
    </div>
  );
};

export default Task4;
